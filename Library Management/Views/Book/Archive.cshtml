@model IEnumerable<Library_Management.Models.BookListViewModel>

@{
    ViewData["Title"] = "Archived Books";
}

<h1 class="mb-4 fw-bold">Archived Books</h1>

<p>
    <a asp-action="Index" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Back to Active Books</a>
</p>

<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-5 g-4">
    @foreach (var book in Model)
    {
        <div class="col">
            <div class="card book-card h-100 shadow-sm border-0">

                <img src="@book.CoverImageUrl" class="card-img-top" alt="@book.Title" style="height: 200px; object-fit: cover; background-color: silver;">

                <div class="card-body">
                    <h6 class="card-title text-truncate" title="@book.Title">@book.Title</h6>
                    <p class="text-muted mb-1 text-truncate">
                        <strong>Author:</strong> @book.AuthorName
                    </p>
                    <p class="mb-0 small text-secondary">
                        @book.Genre | Copies: @book.AvailableCopies / @book.TotalCopies
                    </p>

                    <!-- Restore button -->
                    <form asp-action="RestoreBook" method="post" class="mt-2">
                        <input type="hidden" name="id" value="@book.BookId" />
                        <button type="submit" class="btn btn-success btn-sm w-100" title="Restore">
                            <i class="bi bi-arrow-clockwise"></i> Restore
                        </button>
                    </form>
                </div>
            </div>
        </div>
    }
</div>

@section Scripts {
    <script>
        // Restore book without reload
        document.addEventListener("submit", function(e) {
            const form = e.target;
            if (form.matches("[asp-action='RestoreBook']")) {
                e.preventDefault();
                fetch(form.action, { method: form.method, body: new URLSearchParams(new FormData(form)) })
                    .then(r => {
                        if (!r.ok) throw 'Restore failed.';
                        form.closest('.col')?.remove(); 
                    })
                    .catch(alert);
            }
        });
    </script>
}
